import type { TreeItem } from "@commons/types/tree-view-assets";
import type { Location } from "@commons/types/locations";

export const groupLocationsWithSubLocations = (
  locations: Location[]
): TreeItem[] => {
  return [];
};
